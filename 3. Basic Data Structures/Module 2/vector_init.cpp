#include <bits/stdc++.h>
using namespace std;
int main()
{
    // vector<int> v;      // Vector basic initialization
    // vector<int> v(5);   // 5 length vector (array like)
    vector<int> v(5, 100); // 5 length vector with every cell default value 100
    cout << v.size();
    return 0;
}